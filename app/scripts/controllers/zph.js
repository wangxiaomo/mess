'use strict';

/**
 * @ngdoc function
 * @name gollumApp.controller:ZphCtrl
 * @description
 * # ZphCtrl
 * Controller of the gollumApp
 */
angular.module('gollumApp')
  .controller('zphCtrl', function ($scope, api) {
  
  /*
  api.getZPH().then(function(data){
    $scope.zphList = data;
  });
  */
});
